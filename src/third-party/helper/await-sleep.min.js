import $ from "jquery";
var awaitSleep = {
  sleep(ms) {
    return new Promise(resolve => setTimeout(resolve, ms));
  },
  async redirectTo(url, time = 3000) {
    await this.sleep(time), $(location).attr("href", url);
  }
};
export default awaitSleep;
